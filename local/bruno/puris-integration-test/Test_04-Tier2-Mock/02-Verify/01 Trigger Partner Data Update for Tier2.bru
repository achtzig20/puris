meta {
  name: Trigger Partner Data Update for Tier2
  type: http
  seq: 1
}

get {
  url: {{SUPPLIER_PURIS_BACKEND}}/catena/stockView/update-reported-material-stocks?ownMaterialNumber={{SEMICONDUCTOR_MATERIAL_NUMBER_SUPPLIER}}
  body: none
  auth: none
}

headers {
  X-Api-Key: {{SUPPLIER_PURIS_BACKEND_API_KEY}}
}

tests {
  test("response is ok", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
