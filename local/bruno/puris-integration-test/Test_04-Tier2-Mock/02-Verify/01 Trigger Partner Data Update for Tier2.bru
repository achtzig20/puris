meta {
  name: Trigger Partner Data Update for Tier2
  type: http
  seq: 1
}

post {
  url: {{SUPPLIER_PURIS_BACKEND}}/catena/partner-data-update
  body: json
  auth: none
}

headers {
  X-Api-Key: {{SUPPLIER_PURIS_BACKEND_API_KEY}}
}

body:json {
  {
    "bpnl": "{{TIER2_BPNL}}"
  }
}

tests {
  test("response is ok", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
